{
  "name": "reasixty-exchange",
  "version": "0.1.0",
  "private": true,
  "description": "api.reasixty.com — the Rea-Sixty mapping exchange. Hosts single plugin maps only, never .rea60config bundles.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "mktoken": "node src/tools/mktoken.js",
    "test": "node --test \"test/**/*.test.js\"",
    "check:controls": "python3 ../extension/tools/gen_control_table.py --check"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/multipart": "^9.0.3",
    "@fastify/rate-limit": "^10.2.2",
    "@simplewebauthn/server": "^13.1.1",
    "better-sqlite3": "^11.8.1",
    "fastify": "^5.2.1",
    "nodemailer": "^9.0.3",
    "zod": "^3.24.1"
  }
}

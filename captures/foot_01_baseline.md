# foot_01_baseline

Baseline capture for footswitch decoding (2026-05-19). 5 s on USBPcap3,
SSL 360° running on Windows, UF8 in Plugin-Mixer mode, no user input.

Used as `--baseline` reference for diffing the foot-tap captures.
